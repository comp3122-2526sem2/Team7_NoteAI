FROM postgres:15-alpine

# Set environment variables
ENV POSTGRES_DB=noteai
ENV POSTGRES_USER=noteai
ENV POSTGRES_PASSWORD=noteai

# Expose PostgreSQL port
EXPOSE 5432

# Set default command
CMD ["postgres"]
